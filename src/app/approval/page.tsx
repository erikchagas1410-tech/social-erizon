import { PlaceholderWorkspace } from "@/components/layout/PlaceholderWorkspace";

export default function ApprovalPage() {
  return (
    <PlaceholderWorkspace
      title="Aprovacao"
      subtitle="Camada de decisao para validar o que vai ao ar"
      badge="Operacao"
      copy="Espaco reservado para fila de aprovacao, decisao em lote e validacao final do conteudo antes do agendamento."
    />
  );
}
